---
title: Golden Example Catalog
description: Runnable applied examples and expected results for Retriever.
---
Golden examples are the applied continuation after the core Retriever quickstart. Start with mock-safe commands, inspect the expected result, then opt into camera, simulator, or model-backed lanes only when their dependencies are clear.

<div class="cta-row">
  <a href="/examples/golden-hub-proof/">First Golden proof</a>
  <a href="/examples/perception-memory/">Perception and memory</a>
  <a href="/examples/simulation-visualization/">Visualization lanes</a>
  <a href="/robot-payloads/">Robot payloads</a>
</div>

## Choose By Goal

<div class="choice-grid">
  <a class="choice-card" href="/examples/golden-hub-proof/"><span>Start</span><strong>I want the smallest proof</strong><p>Run the Hub pack smoke with no robot, camera, model, simulator, or network dependency.</p><code>pixi run demo-golden-hub-pack</code></a>
  <a class="choice-card" href="/examples/perception-memory/"><span>Learn</span><strong>I want the applied ladder</strong><p>Walk perception, memory, language, and composition using deterministic payloads first.</p><code>pixi run -e golden-local demo-perception-detection-flow</code></a>
  <a class="choice-card" href="/examples/simulation-visualization/"><span>Inspect</span><strong>I want visual artifacts</strong><p>Render graph structure, simulator traces, and optional Rerun streams after the mock path works.</p><code>pixi run demo-pipeline-html-viz</code></a>
  <a class="choice-card" href="/robot-payloads/"><span>Reuse</span><strong>I need reusable robot payloads</strong><p>Use robot state, belief, plan, trajectory, event-buffer, and dataset export profiles across examples.</p><code>pixi run demo-robotics-typing-catalog</code></a>
</div>

## Walkthrough Order

1. Run the Hub proof to check that Golden can extend Retriever without importing optional robot dependencies.
2. Run one deterministic perception or memory demo so you can recognize expected output before adding heavier inputs.
3. Render the pipeline graph when the behavior is confusing; inspect ports and edges before changing code.
4. Only then opt into camera, model-backed perception, simulator, or dataset-export lanes.

## First Verified Commands

| Command | Expected result | Why it comes first |
| --- | --- | --- |
| `pixi run demo-golden-hub-pack` | Prints Golden pack exports, registry lookup, constructed payloads, and Arrow round-trip. | Shows Golden extending Retriever through a manifest. |
| `pixi run -e golden-local demo-perception-detection-flow` | Steps a deterministic synthetic perception graph to completion. | Proves the concise example ladder before heavier integrations. |
| `pixi run demo-robosuite-mock` | Prints `[mock step=...]` simulator-policy trace lines. | Proves environment-as-Flow and policy-as-Flow without robosuite installed. |
| `pixi run demo-pipeline-html-viz` | Prints an ASCII graph and writes `out/golden_retriever_closed_loop_viz.html`. | Proves IR validation and graph inspection. |

<figure class="figure-card">
  <img src="/assets/robot-agent-graph-ultralight.png" alt="Retriever robot agent graph with observe, belief, planner, monitor, skill, controller, and environment flows" />
  <figcaption><strong>Expected graph shape.</strong> Golden examples make the closed-loop robot pattern concrete without hiding timing and handoff boundaries in callbacks.</figcaption>
</figure>

## Example Families

<div class="card-grid">
  <a class="info-card" href="/examples/golden-hub-proof/"><strong>Hub proof</strong><span>Load Golden exports through Retriever Hub and the unified runtime registry.</span></a>
  <a class="info-card" href="/examples/perception-memory/"><strong>Perception and memory</strong><span>Detection, segmentation, pointing, belief updates, dropout memory, and remembered pointing.</span></a>
  <a class="info-card" href="/examples/language-grounding/"><strong>Language and grounding</strong><span>Captions, grounded references, and primitive plan text over typed observations.</span></a>
  <a class="info-card" href="/examples/pipeline-composition/"><strong>Pipeline composition</strong><span>Registry-backed composition and pipeline-as-Flow surfaces.</span></a>
  <a class="info-card" href="/examples/simulation-visualization/"><strong>Simulation and visualization</strong><span>Rerun, mock-safe robosuite, TWIST2/MuJoCo lanes, and HTML graph visualization.</span></a>
  <a class="info-card" href="/robot-payloads/"><strong>Robot payloads</strong><span>World state, belief, plans, trajectories, event streams, and dataset/export profiles.</span></a>
</div>

## Runtime Bridge

<div class="reference-strip">
  <strong>Need the runtime mental model?</strong> Use the <a href="https://openretriever-docs.pages.dev/">core Retriever docs</a> for Flow, Pipeline, clocks, sync policies, graph rendering, stepping, and replay. Golden pages assume those runtime ideas and focus on robot-facing examples, payloads, and Hub pack candidates.
</div>

## Maturity Levels

| Level | Meaning | User expectation |
| --- | --- | --- |
| Hub-loadable pack | Declared by the Hub manifest and loaded by Retriever Hub. | Safe to import and reuse as a pack boundary. |
| Promoted demo | Has a named Pixi task, docs page, expected output, and smoke coverage. | Safe for first-run docs and CI checks. |
| Source reference | Useful implementation pattern, but not yet a public launch point. | Read the source after the promoted path works. |
| Optional integration | Requires camera, model, simulator, GPU, robot, or external service. | Use only when the dependency story is explicit. |
