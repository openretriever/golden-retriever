"""Planner implementations for retriever_tamp.

This package intentionally starts empty. It exists so the first concrete symbolic
planner port has a stable landing zone without forcing that implementation into
`symbolic.base`.
"""
