# Public metadata for GoldenRetriever, the companion examples and system-demo
# repository for the core Retriever runtime. The heavy optional stacks remain
# opt-in so the default public path stays documentation- and example-first.

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[project]
name = "retriever-golden"
version = "0.0.0"
description = "Advanced examples and system integrations built on the Retriever runtime."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "OpenRetriever" }]
keywords = ["robotics", "retriever", "examples", "closed-loop", "agents"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.11",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

[project.urls]
Homepage = "https://github.com/openretriever/golden-retriever"
Documentation = "https://openretriever.github.io/golden-retriever/"
Repository = "https://github.com/openretriever/golden-retriever"
Issues = "https://github.com/openretriever/golden-retriever/issues"

[project.optional-dependencies]
# Legacy/system stack (carried over from the current monorepo; expect to refine).
golden = [
    # Core ML & Framework
    "transformers",
    "openai",
    "accelerate",
    "google-generativeai",
    "pillow-heif",
    "pyheif",

    # Core Services & Utils
    "ray[all]",
    "langchain",
    "langchain_openai",

    # Core Model Deployment Packages
    "supervision",
    "ultralytics==8.3.40",
    "ultralytics-thop==2.0.12",
    "einops",
    "pycocotools",
    "sam2",

    # Configuration
    "hydra-core",
    "omegaconf",

    # Utilities
    "click",
    "rich",
    "ftfy",
    "regex",
    "tqdm",
    "spatialmath-python",
    "pydantic>=2.0.0",
    "fastapi>=0.100.0",
    "uvicorn[standard]",
    "httpx",
    "pymongo",
    "pytorch_lightning",

    # Data Processing & Utils
    "protobuf==3.20.*",  # For wandb compatibility
    "packaging",
    "Pillow",
    "numpy<2.0.0",
    "scipy",
    "pandas",
    "seaborn>=0.13.2",
    "py-cpuinfo>=9.0.0",

    # Other
    "rerun",
    "wandb",
    "bitsandbytes",
    "gym",

    # New dependencies
    "dspy-ai>=2.4.0",
    "python-dotenv",
]

# Refer to requirements-training.txt (legacy).
training = [
    "absl-py",
    "accelerate",
    "aiohttp",
    "aiosignal",
    "async-timeout",
    "attrs",
    "bitsandbytes",
    "charset-normalizer",
    "cmake",
    "datasets",
    "deepspeed",
    "einops",
    "evaluate",
    "frozenlist",
    "fsspec",
    "huggingface-hub",
    "idna",
    "multidict",
    "ninja",
    "psutil",
    "responses",
    "safetensors",
    "sentencepiece",
    "tokenizers",
    "torch",
    "tqdm",
    "transformers",
    "triton",
    "xxhash",
    "yarl",
]

mapper = ["dgl", "open3d", "lxml", "PyMCubes", "trimesh", "pyglet==1.5.27"]

spot = [
    "bosdyn-client",
    "bosdyn-core",
    "bosdyn-api",
    "bosdyn-mission",
    "bosdyn-choreography-client",
]

vlm_planning = [
    "litellm",
    "torch",
    "gymnasium",
    "typing",
]

dev = [
    "black",
    "ruff",
    "mypy",
    "pytest>=7.2.2",
    "pytest-asyncio",
    "pre-commit",
    "pytest-cov",
]
