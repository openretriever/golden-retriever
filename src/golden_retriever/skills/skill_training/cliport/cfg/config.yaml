# Shared Config

root_dir: ${oc.env:CLIPORT_ROOT} # set this ENV variable if you didn't `python setup.py develop`

#root_dir: ${env:CLIPORT_ROOT} #This one is not work for new hydra


tag: default
debug: False
