"""Runnable GoldenRetriever demos exposed through Retriever Hub.

These are the applied, one-line-runnable examples (as opposed to the reusable
payload types in the rest of ``retriever_typing``). Kept in their own subpackage
so importing the types never pulls in demo-only dependencies like OpenCV.
"""
