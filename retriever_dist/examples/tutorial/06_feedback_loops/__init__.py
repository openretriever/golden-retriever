"""Feedback-loop tutorial examples (runtime-aligned).

These are rewrites/extractions of legacy `examples/legacy/06_feedback_loops/*`
but implemented on top of the refactored runtime (`Pipeline`, `Rate`, `Trigger`,
adapters, and explicit lag policy on `Rate`).
"""

